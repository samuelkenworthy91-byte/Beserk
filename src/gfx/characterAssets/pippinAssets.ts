// ─────────────────── pippin external assets ───────────────────
//
// TODO: drop in pippin.png sheets under public/sprites/battle/,
// public/sprites/map/, public/portraits/ and replace the bodies of
// registerPippinAssets() and preloadPippinAssets().

export function registerPippinAssets(): void {
  // no-op — sheets not yet authored
}

export async function preloadPippinAssets(): Promise<void> {
  // no-op
}

export function _resetPippinRegistration(): void {
  // no-op
}
