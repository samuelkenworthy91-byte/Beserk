// ─────────────────── merc external assets ───────────────────
//
// TODO: drop in merc.png sheets under public/sprites/battle/,
// public/sprites/map/, public/portraits/ and replace the bodies of
// registerMercAssets() and preloadMercAssets().

export function registerMercAssets(): void {
  // no-op — sheets not yet authored
}

export async function preloadMercAssets(): Promise<void> {
  // no-op
}

export function _resetMercRegistration(): void {
  // no-op
}
