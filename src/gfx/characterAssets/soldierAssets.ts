// ─────────────────── soldier external assets ───────────────────
//
// TODO: drop in soldier.png sheets under public/sprites/battle/,
// public/sprites/map/, public/portraits/ and replace the bodies of
// registerSoldierAssets() and preloadSoldierAssets().

export function registerSoldierAssets(): void {
  // no-op — sheets not yet authored
}

export async function preloadSoldierAssets(): Promise<void> {
  // no-op
}

export function _resetSoldierRegistration(): void {
  // no-op
}
